Project (aioz-world-deck) unit testing: Vitest preferred (no Jest). Tests co-located as *.test.ts/*.test.tsx next to source. Must be fast, in-memory only — mock all HTTP via axios-mock-adapter, mock redux-persist storage, use jsdom for components. No real Next.js server, no real filesystem, no real network. Test layers: Zod schemas (pure), hooks (mock TanStack Query), services (mock adapter), stores (in-memory store), utils (pure), components (RTL). Skip: generated SDK, Next.js routing, third-party library internals, pure UI wrappers.
§
User preference: all project-specific knowledge should live as docs/ files IN the project, not in profile-level ByteRover context tree or Hermes memory. Profile storage is for agent-facing ephemeral context only. When brv context tree accumulates project knowledge, export to docs/architecture/ with category--topic naming.